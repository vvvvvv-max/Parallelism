# ZeRO 三阶段详解

ZeRO（Zero Redundancy Optimizer）是微软 DeepSpeed 提出的显存优化技术，
核心思想是**消除数据并行中的显存冗余**。

## 背景：数据并行的显存冗余

标准数据并行下，每张卡都存一份完整的：
- 模型参数（Parameters）
- 梯度（Gradients）
- 优化器状态（Optimizer States，如 Adam 的 m、v）

假设模型有 Φ 个参数，用 Adam 优化器（fp16 训练 + fp32 优化器状态）：

| 组成 | 显存占用 |
|------|---------|
| fp16 参数 | 2Φ |
| fp16 梯度 | 2Φ |
| fp32 参数副本 | 4Φ |
| fp32 动量 m | 4Φ |
| fp32 动量 v | 4Φ |
| **合计** | **16Φ** |

一个 7B 模型，光模型状态就要 **112 GB**，远超单卡 80GB。

## ZeRO 三阶段

### ZeRO-1：切分优化器状态（Optimizer States）

- **切分对象**：Adam 的 m、v 和 fp32 参数副本
- **每卡显存**：`2Φ + 2Φ + (4Φ+4Φ+4Φ)/N = 4Φ + 12Φ/N`
- **N 卡时节省**：优化器状态部分
- **通信**：与标准 DP 相同（all-reduce 梯度）

**原理**：每张卡只维护 1/N 的优化器状态，更新参数时通过 all-gather 收集。

### ZeRO-2：切分梯度（Gradients）

- **切分对象**：在 ZeRO-1 基础上，再切分梯度
- **每卡显存**：`2Φ + 2Φ/N + 12Φ/N = 2Φ + 14Φ/N`
- **N 卡时节省**：优化器状态 + 梯度
- **通信**：与 ZeRO-1 相同

**原理**：每张卡只保留自己负责的那部分参数的梯度，用 reduce-scatter 代替 all-reduce。

### ZeRO-3：切分模型参数（Parameters）

- **切分对象**：在 ZeRO-2 基础上，再切分模型参数
- **每卡显存**：`(2Φ + 2Φ + 12Φ)/N = 16Φ/N`
- **N 卡时节省**：全部三部分
- **通信**：前向 / 反向需要 all-gather 参数，通信量增加约 1.5 倍

**原理**：每张卡只存 1/N 的参数，计算时临时 all-gather 需要的层，用完释放。

## 三阶段对比

| 阶段 | 切分对象 | 每卡显存 | 通信开销 |
|------|---------|---------|---------|
| 标准 DP | 无 | 16Φ | 1x |
| ZeRO-1 | 优化器状态 | 4Φ + 12Φ/N | 1x |
| ZeRO-2 | + 梯度 | 2Φ + 14Φ/N | 1x |
| ZeRO-3 | + 参数 | 16Φ/N | 1.5x |

## 举例：7B 模型在 8 卡上

| 策略 | 每卡显存 |
|------|---------|
| 标准 DP | 112 GB（装不下） |
| ZeRO-1 | 4×7 + 12×7/8 ≈ 38.5 GB |
| ZeRO-2 | 2×7 + 14×7/8 ≈ 26.25 GB |
| ZeRO-3 | 16×7/8 = 14 GB |

## 一句话总结

ZeRO 通过**逐级切分优化器状态 → 梯度 → 参数**，把数据并行的显存冗余消除干净，
代价是 ZeRO-3 阶段通信量增加。实际训练中常用 ZeRO-2（性价比最高）或 ZeRO-3 + offload。